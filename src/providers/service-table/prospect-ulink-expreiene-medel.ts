export class ProspectUlinkExpreieneModel{
    agentid: string; //(Require)
    customerid: string; //(Require)
    deposit: string; // Y,N (Require)
    governmentbonds: string;// Y,N (Require)
    fixedincome: string;// Y,N (Require)
    stock: string;// Y,N (Require)
    status: string;
    lastupdate: string;
}